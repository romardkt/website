<div class="row">
    <div class="col-xs-12 text-center">
        <div class="btn-group">
            <a class="btn btn-default{{{ ($state == 'who') ? ' active' : ''  }}}">1. Register As</a>
            <a class="btn btn-default{{{ ($state == 'info') ? ' active' : '' }}}">2. Info</a>
            <a class="btn btn-default{{{ ($state == 'contacts') ? ' active' : '' }}}">3. Contacts</a>
            <a class="btn btn-default{{{ ($state == 'league') ? ' active' : '' }}}">4. League</a>
            <a class="btn btn-default{{{ ($state == 'finish') ? ' active' : '' }}}">5. Finish</a>
        </div>
    </div>
</div>
<div class="row">
    <div class="col-xs-12 text-center">
        &nbsp;
    </div>
</div>
